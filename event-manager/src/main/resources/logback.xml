<configuration>
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender (optional) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/application-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Application Logger -->
    <logger name="org.example.ticketmaster.eventmanager" level="DEBUG" />

    <!-- MongoDB Driver - Set to WARN to reduce noise -->
    <logger name="org.mongodb" level="WARN" />
    <logger name="com.mongodb" level="WARN" />

    <!-- Elasticsearch - Set to WARN to reduce noise -->
    <logger name="org.elasticsearch" level="WARN" />
    <logger name="co.elastic" level="WARN" />

    <!-- Apache HTTP Client - Set to WARN to reduce noise -->
    <logger name="org.apache.http" level="WARN" />

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <!-- Uncomment to enable file logging -->
        <!-- <appender-ref ref="FILE" /> -->
    </root>
</configuration>